## 1. 原图预览导航

- [x] 1.1 将 `ImageReviewModal` 的原图预览状态改为索引驱动，并由索引派生当前预览原图
- [x] 1.2 为原图覆盖层实现 `↑ / ↓` 切换上一张/下一张原图，并在切换时重置 `sourceZoom`
- [x] 1.3 调整原图覆盖层的键盘事件优先级，确保 `Esc` 只关闭原图覆盖层，且 `← / →` 不会在覆盖层打开时误切主审核图

## 2. 审核纠错交互

- [x] 2.1 调整审核弹窗操作按钮渲染，让 `pending` 保持现有三种操作、`approved` 支持“改为驳回”、`rejected` 支持“改为通过”
- [x] 2.2 重构弹窗审核提交流程，区分“pending 首次审核”与“已审核结果纠错”，避免纠错后误触发自动关闭或自动跳转
- [x] 2.3 校验 `PATCH /api/admin/images/[id]/review` 在纠错路径上的行为，确保改为 `rejected` 时仍要求理由，改为 `approved` 时清空 `rejectionReason`

## 3. 页面集成与状态同步

- [x] 3.1 更新 `review-client.tsx` 与弹窗回调之间的本地状态同步，确保纠错后列表状态、统计卡片和弹窗状态一致
- [x] 3.2 更新弹窗内按钮文案、快捷键提示和原图预览提示，使新导航与纠错行为对管理员可见

## 4. 验证

- [x] 4.1 为原图覆盖层上下切换和审核纠错路径补充自动化测试，或补齐最小可执行的交互回归覆盖
- [x] 4.2 手工验证审核流程：原图放大后上下切换、pending 图片首审自动翻页、approved 改驳回、rejected 改通过
