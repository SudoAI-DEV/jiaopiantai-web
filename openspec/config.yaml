schema: spec-driven

context: |
  Tech stack: TypeScript, Next.js, React, Node.js, Drizzle ORM, PostgreSQL.
  Domain: e-commerce AI product image generation platform.
  Environment:
  - Local development line test DB host/IP: 192.168.88.88
  - Use this environment detail when planning or validating DB-related changes in OpenSpec artifacts.

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours
#     general:
#       - 禁止使用 db:push，必须使用 db:migrate
